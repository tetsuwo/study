/*!
 * Main
 *
 */

function Main() {
}

// ----

function log(text) {
    var field = document.getElementById('log');
    field.innerHTML = text + "\n" + field.innerHTML;
}

// ----

var main = new Main();
